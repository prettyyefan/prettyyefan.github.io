# 首次使用或更新依赖（安装 TailwindCSS 等）
pnpm install

# 本地预览（在项目根目录执行）
hugo server
# 或者使用 package.json 中定义的脚本
pnpm dev

# 构建网站
hugo --minify
# 或者
pnpm build

# 更新内容后推送
git add .
git commit -m "update 2025.12.01"
git push
# 部署指南

## 部署到GitHub Pages

### 步骤1: 在GitHub上创建仓库

1. 访问 [GitHub](https://github.com/) 并登录
2. 点击右上角的 "+" 按钮,选择 "New repository"
3. 仓库名称填写: `<你的GitHub用户名>.github.io`
   - 例如,如果你的用户名是 `yefan`,则填写 `yefan.github.io`
4. 选择 "Public" (公开仓库)
5. 不要勾选 "Initialize this repository with a README"
6. 点击 "Create repository"

### 步骤2: 推送代码到GitHub

在你的本地项目目录 `academic-site` 中执行以下命令:

```bash
# 添加远程仓库(替换为你的GitHub用户名)
git remote add origin https://github.com/<你的用户名>/<你的用户名>.github.io.git

# 重命名分支为main(如果当前是master)
git branch -M main

# 推送代码到GitHub
git push -u origin main
```

### 步骤3: 配置GitHub Pages

1. 进入你的GitHub仓库页面
2. 点击 "Settings" (设置)
3. 在左侧菜单中找到 "Pages"
4. 在 "Build and deployment" 部分:
   - Source: 选择 "GitHub Actions"
5. 保存设置

### 步骤4: 等待部署完成

1. 点击仓库顶部的 "Actions" 标签
2. 你会看到一个正在运行的工作流 "Deploy Hugo site to Pages"
3. 等待工作流完成(通常需要1-3分钟)
4. 完成后,你的网站将在 `https://<你的用户名>.github.io` 上线

## 更新网站内容

每次修改内容后:

```bash
# 添加修改的文件
git add .

# 提交修改
git commit -m "update 2025.11.29"

# 推送到GitHub
git push
```

GitHub Actions会自动重新构建并部署你的网站。

## 自定义域名(可选)

如果你有自己的域名:

1. 在仓库的 `static` 目录下创建一个名为 `CNAME` 的文件
2. 在文件中写入你的域名,例如: `www.yourname.com`
3. 在你的域名提供商处添加DNS记录:
   - 类型: CNAME
   - 名称: www (或 @)
   - 值: `<你的用户名>.github.io`
4. 在GitHub仓库设置的Pages页面中,在 "Custom domain" 填入你的域名
5. 勾选 "Enforce HTTPS"

## 本地预览

在推送到GitHub之前,你可以在本地预览:

```bash
# 启动本地服务器
hugo server

# 在浏览器中访问
http://localhost:1313
```

## 常见问题

### Q: 网站没有显示样式
A: 确保 `config/_default/hugo.yaml` 中的 `baseURL` 设置正确为你的GitHub Pages URL。

### Q: 推送后网站没有更新
A: 检查GitHub Actions是否成功运行,查看Actions标签页中的日志。

### Q: 图片无法显示
A: 确保图片路径正确,并且图片文件已经提交到Git仓库。

### Q: 如何添加新的论文/博客文章?
A: 在相应的目录下创建新的文件夹和 `index.md` 文件,参考现有的示例。

## 技术支持

- Hugo文档: https://gohugo.io/documentation/
- Hugo Blox文档: https://docs.hugoblox.com/
- GitHub Pages文档: https://docs.github.com/pages

## 重要提示

⚠️ **在推送到GitHub之前**:

1. 更新 `config/_default/hugo.yaml` 中的 `baseURL` 为你的实际GitHub Pages URL
2. 更新 `content/authors/admin/_index.md` 中的个人信息
3. 替换 `content/authors/admin/avatar.png` 为你的头像
4. 更新邮箱、社交媒体链接等个人联系方式
5. 删除或修改示例内容(博客文章、论文等)

祝你的学术主页搭建顺利! 🎉

